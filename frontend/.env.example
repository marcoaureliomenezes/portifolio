# Analytics SDK endpoint — set per environment in CI/CD (stage/prod).
# Do NOT commit a real value here. Leave empty locally; the SDK no-ops when empty.
# Example: https://api.dadaia.dev/ingest
VITE_ANALYTICS_ENDPOINT=
